# Build artifacts
build/
dist/
__pycache__/
*.pyc

# ADB / tools binaries (needed to run, not to track)
adb.exe
fastboot.exe
AdbWinApi.dll
AdbWinUsbApi.dll
ApkIconHelper.apk
scrcpy-win64-*.zip
Drivers/
MTK_CDC_Driver/

# scrcpy runtime extraction artifacts (duplicates of scrcpy-win64-v3.3.4/)
SDL2.dll
avcodec-*.dll
avformat-*.dll
avutil-*.dll
libusb-*.dll
swresample-*.dll
scrcpy.exe
scrcpy-server
scrcpy-console.bat
scrcpy-noconsole.vbs
open_a_terminal_here.bat
icon.png

# Installers / logs
GitHubDesktopSetup-*.exe
pyarmor.bug.log

# Older / duplicated data
gelotech_database_v2.json
update_server/

# Settings shipped with exe (kept at repo root for the update pull)
# dist/secret.json is intentionally ignored via dist/

# Runtime settings generated by local runs
exclusions.json

# Cloudflare Worker (worker/)
worker/node_modules/
worker/.wrangler/
